# Runtime data — generated by docker init containers, never commit
execution/geth/
execution/genesis.json
execution/jwtsecret
execution/keystore/
consensus/beacondata/
consensus/validatordata/
consensus/genesis.ssz

# Static configs — MUST be committed (required for L1 bootstrap)
# execution/genesis-raw.json
# consensus/config.yml
